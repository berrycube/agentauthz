"""AgentAuthZ — a deliberately vulnerable AI agent.

A deliberately-vulnerable business AI agent (customer-service domain) plus a
red-team harness, built for security education. The vulnerabilities are
business-logic / authorization flaws (ownership, session authz, approval gates)
— intentional and reproducible BY DESIGN, in the spirit of OWASP Juice Shop /
DVWA, but targeting an AI agent's tool-calling layer for the first time.
"""

__version__ = "0.0.0"
